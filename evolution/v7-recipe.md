# Research Note — Round 3 (v6): validated wins, one under-powered value claim, and the v7 recipe

**Status:** research note only. No adoption. `premortem-evolved` v2 remains installed and unchanged (md5-verified). Round record: `ab-test-results-round3.md`; gate: `pre-registration.md`.

## 1. Validated in round 3 (blind, pre-registered, fresh slate/fixture/judges)

| Delta | Evidence |
|---|---|
| **De-ordinalized template + prose (Delta A)** | Machinery-leak census ~19 items/9 champion runs vs ~1/9 v6; the champion's stage-leak defect reproduced on fresh judges (5/9 runs, incl. 4× "Stage 4/5" citations to nonexistent sections in one report) and v6 eliminated the class. D9: v6 ≥ champion on judges (+0.42/+0.39, two independent panels) and on the pre-registered census (4.89 vs 3.33) |
| **Checklist item 18 contradiction fix + item 19 mechanical scan (Delta A)** | Zero screen-narration leaks in any v6 run (v5 had leaked in 3 of 5) |
| **Tie-pair enumeration (Delta B)** | Ladder violations 3 vs 9; five v6 runs restructured to ≤1 two-way tie; D3 auditor-clean runs 6/9 vs 0/9 |
| **Proportionality screen (inherited v4/v5)** | Deflated an unseen fixture (podcast migration) with correct real gotchas while champion ran 3,861-word ceremony — replication on an uncompromised fixture |
| **Tiers/falsifiers/prevent-limit-accept/re-upgrade rows/calibration line (inherited)** | D5 +1.22 (largest margin in any round, either project); 12/12 forced choices; every judge cited the rebuild machinery |
| **Detection stack** | Keys 97.2% vs 93.1% strict; one *unplanted* real statutory catch (Oregon agritourism ≈18-event ceiling vs the plan's 24-event success definition) |

## 2. Why v6 was still not adopted

D7 (+0.67 then +0.78) cleared its floor twice but landed inside the pre-registered noise band twice (margin < candidate within-arm spread 1.0). The addendum's closure rule ended the round: NO ADOPTION, insufficient evidence at n=9 runs/arm. The paradox on record: v6's D7 cells vs champion's uniform 6s are 14 higher / 4 equal / 0 lower (one-sided sign test p ≈ 6×10⁻⁵) — the effect is almost certainly real; the *registered statistic* (direction-blind spread heuristic) cannot see one-directional dominance. The bar was mis-set, and per protocol it binds anyway and may only be re-registered for a future round.

## 3. The v7 recipe (fully specified; running it is a fresh decision)

v7 = v6 + exactly one skill delta, tested under two re-registered harness bars:

1. **Skill delta — two-way ties only.** Current rule text ("at most one tie") lets executors attempt three-way ties that the mechanical reading then rejects (the residual violation class in BOTH arms, 5 of 6 r3 runs). Change the ladder rule and checklist item 5 to: "a tie spans exactly two modes; a third mode may never share the rung — re-ladder it." Expected: D3 auditor-clean 9/9.
2. **Harness re-registration A — value-dim noise test.** Replace the spread heuristic with the protocol's paired test at brief level: value floor passes iff margin ≥ floor AND a one-sided paired sign test across judge-score cells gives p < 0.01 AND no cell regresses. (Rationale: recorded in §2; decided BEFORE any v7 run, as required.)
3. **Harness re-registration B — auditor prompt fidelity.** The auditor prompt must quote the pre-registered census verbatim; any category not in the pre-registration is scored separately as an exploratory census, never in the binding score (round-3 deviation, adjudication note 1).
4. **Keep:** everything else — gate structure, briefs G/H/I are now burned (three exposures), so a v7 round needs a fresh slate; the podcast fixture has ONE clean use left before it risks the newsletter-fixture fate (never quote its answer in any skill text).

## 4. Harness lessons (this round's contribution to the gauntlet)

1. **Header-ordinal stripping in the blinder works** — 11 files needed it, all champion-side + controls; without it every judge could have identified arms by header style. Adopt as standard when arms differ in template format.
2. **A fixture is burned the moment its answer enters the skill text.** The Kimi newsletter fixture is quoted verbatim inside v4/v5's screen example; restraint on it can never be measured again. Rotate fixtures; never let a fixture's content into any candidate.
3. **Census/anchor cross-contamination is the auditor's failure mode:** a "machinery" census that includes rubric-required content (tier labels, calibration lines) charges the same ink twice with opposite signs. Define censuses by exclusion lists tied to the rubric's anchors.
4. **The cheap noise band is direction-blind** (§2). Use it only where distributions genuinely overlap; pre-register the paired sign test whenever an arm could plausibly dominate cell-wise.
5. **Namespace hygiene:** blind codes must be disjoint from judge IDs and dimension names across ALL rounds (J3 collision this round; D5/D9 collisions in the Kimi rounds).
